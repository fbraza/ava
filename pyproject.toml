[project]
name = "iva-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "python-dotenv",
    "slack-sdk",
    "pydantic-ai",
    "httpx"
]

[dependency-groups]
dev = [
    "pytest",
    "pre-commit",
    "coverage",
    "ruff",
    "ipython>=8.37.0",
]
